# =============================================================================
# LiteSAM-3D Requirements
# Tested on: Python 3.10-3.12, CUDA 12.1, RTX 3090 (24GB)
# Install: pip install -r requirements.txt
# =============================================================================

# --- Core Deep Learning ---
torch>=2.1.0
torchvision>=0.16.0
torchaudio>=2.1.0          # needed for some torch utilities

# --- Vision Transformers & Model Zoo ---
timm>=0.9.12               # TinyViT backbone (tiny_vit_5m_224, tiny_vit_21m_224)
einops>=0.7.0              # Elegant tensor operations (rearrange, repeat)

# --- Medical Image I/O ---
nibabel>=5.1.0             # NIfTI file loading (.nii, .nii.gz) — AMOS22, etc.
SimpleITK>=2.3.1           # Advanced medical image I/O and resampling
pydicom>=2.4.3             # DICOM file support

# --- Medical DL Framework ---
monai>=1.3.0               # Medical image transforms, metrics, data loading

# --- Scientific Computing ---
numpy>=1.24.0
scipy>=1.11.4              # Hausdorff distance, distance transforms
scikit-image>=0.21.0       # Image processing utilities

# --- Data Augmentation ---
albumentations>=1.3.1      # Fast image augmentation pipeline
opencv-python>=4.8.1       # Image ops required by albumentations

# --- Config Management ---
omegaconf>=2.3.0           # Hierarchical YAML config (hydra-compatible)
hydra-core>=1.3.2          # Optional: full hydra support for config sweeps

# --- Experiment Tracking ---
wandb>=0.16.0              # Weights & Biases (recommended)
tensorboard>=2.15.0        # TensorBoard (alternative)
tqdm>=4.66.0               # Progress bars

# --- Utilities ---
rich>=13.7.0               # Beautiful terminal output / logging
pandas>=2.1.0              # CSV results logging
matplotlib>=3.8.0          # Plotting (loss curves, segmentation overlays)
seaborn>=0.13.0            # Statistical plots (ablation results)
Pillow>=10.1.0             # Image I/O utilities

# --- Code Quality (optional but recommended) ---
black>=23.0.0              # Code formatter
isort>=5.12.0              # Import sorter
